name: DefXBiometric
options:
  bundleIdPrefix: com.definex
  deploymentTarget:
    iOS: "12.0"
  
settings:
  base:
    SWIFT_VERSION: "5.7"
    BUILD_LIBRARY_FOR_DISTRIBUTION: YES
    SKIP_INSTALL: NO
    IPHONEOS_DEPLOYMENT_TARGET: "12.0"

targets:
  DefXBiometric:
    type: framework
    platform: iOS
    sources:
      - path: Sources/DefXBiometric
        excludes:
          - "Resources/**"
    settings:
      base:
        PRODUCT_NAME: DefXBiometric
        PRODUCT_BUNDLE_IDENTIFIER: com.definex.DefXBiometric
        DEFINES_MODULE: YES
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        GENERATE_INFOPLIST_FILE: YES
    scheme:
      testTargets: []
      gatherCoverageData: false

schemes:
  DefXBiometric:
    build:
      targets:
        DefXBiometric: all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

